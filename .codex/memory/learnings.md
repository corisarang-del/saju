# Learnings

## 2026-07-01: 디자인 개선 재리뷰 결과

**확인 내용**:
- 모바일 하단 쿠키 배너가 입력/CTA를 가리던 P0는 해결됐다.
- 쿠키 배너 개인정보 링크는 i18n 라우팅으로 `/ko/privacy-policy` 흐름을 유지한다.
- 첫 랜딩 캐릭터 이미지 LCP 경고는 `priority`, `loading="eager"`, `fetchPriority="high"` 적용 후 재현되지 않았다.
- 무료 혜택 문구는 `3별 무료`보다 `3회 무료`가 사용자 이해에 좋다.
- 새 디자인 회귀 테스트는 직접 바이너리 기준 2 files / 15 tests 통과했다.
- `pnpm exec vitest ...`는 기존 환경 이슈와 동일하게 `[ERROR] fetch failed`로 실패했다.

**남은 이슈**:
- 모바일에서 쿠키 배너를 상단 fixed overlay로 옮기면서 `/ko` H1과 `/ko/reading` 제목 영역을 덮는다.
- 기능 조작 차단은 아니지만 첫 화면 정보 위계와 제품 첫인상을 해치는 P1로 본다.

**교훈**:
- 하단 입력 차단을 피하려고 상단 overlay로 옮기면 제목/헤더 가림 문제가 생길 수 있다.
- 쿠키 안내는 fixed overlay보다 inline notice 또는 공간 예약형 배너가 모바일 첫 화면에 더 안정적이다.
- 디자인 회귀 테스트는 문자열 검사만으로 충분하지 않고 390x844 시각 겹침 검증이 필요하다.

---

## 2026-06-30: 디자인 감사 핵심 리스크

**확인 내용**:
- 모바일에서 쿠키 배너가 `/ko/reading`의 `다음` 버튼과 랜딩 CTA/가격 카드를 가려 전환을 직접 방해한다.
- 전역 토큰은 다크 퍼플인데 랜딩은 베이지/티얼, 입력 폼은 토스 블루라서 브랜드 일관성이 약하다.
- 같은 사주 정보 입력이 `/reading`과 채팅 안 입력 카드에서 다른 UI/이름 입력 방식을 사용한다.
- 생년월일 숫자 입력과 캐릭터 캐러셀 점 버튼에 접근성 이름이 부족하다.
- 후기 섹션은 “실제 카톡 대화”, 극적인 맞춤 사례, 과한 구어체 때문에 신뢰보다 조작감을 줄 수 있다.

**개발자 인수인계**:
- 상세 티켓은 `docs/pm/디자인-감사-개발자-수정-가이드.md`에 정리했다.

**교훈**:
- 이 프로젝트의 디자인 수정은 미감보다 전환 방해 제거와 신뢰 형성이 우선이다.
- 별 과금 단위, primary CTA, 입력 폼은 화면마다 같은 시각 언어를 써야 한다.
- 모바일 390x844 기준으로 쿠키 배너, 하단 탭, CTA 겹침을 반드시 확인해야 한다.

---

## 2026-06-30: PM 리뷰 기준 공개 런칭 리스크

**확인 내용**:
- 비공개 베타는 가능하지만 결제 포함 공개 런칭 전에는 보안, 결제, 법무, 빌드 게이트 점검이 필요하다.

**주요 리스크**:
- 개인정보 접근 경계: `readingId` 기반 조회는 서버 코드에서도 `user_id` 소유자 조건을 명시하는 편이 안전하다.
- 결제/별 차감: 원자성, 중복 웹훅, 거래 이력 정합성 확인이 필요하다.
- 법무: 실제 사업자 정보 확정 전에는 가짜 사업자번호를 노출하면 안 된다.
- 빌드: Google Fonts fetch 실패 리스크가 있어 로컬 폰트 전환 검토가 필요하다.
- 린트: React 19 규칙 위반이 기존 파일에 남아 있다.

---

## 2026-06-30: pnpm 실행 환경 이슈

**확인 내용**:
- `pnpm test`, `pnpm lint`, `pnpm --version` 실행이 현재 환경에서 `fetch failed` 또는 응답 없음으로 불안정했다.
- 로컬 바이너리 직접 실행은 동작했다.

**검증 대안**:
- `./node_modules/.bin/vitest run`
- `./node_modules/.bin/tsc --noEmit`
- `./node_modules/.bin/eslint`

---

## 2026-06-30: 검증 게이트 차단점

**확인 내용**:
- 테스트와 타입체크는 직접 바이너리 기준 통과했지만, 배포 전 품질 게이트는 아직 통과 상태가 아니다.
- ESLint는 14 errors / 29 warnings로 실패한다.
- Next 빌드는 Google Fonts `Noto Sans KR` fetch 실패로 중단된다.
- `package.json`의 `test:env`가 가리키는 `scripts/check-env.js`는 존재하지 않는다.

**개발자 인수인계**:
- 상세 티켓은 `docs/pm/개발자-에이전트-검증-이슈-인수인계.md`에 정리했다.

**교훈**:
- 성공 여부를 말할 때는 `pnpm` 스크립트 실행 결과와 직접 바이너리 실행 결과를 구분해야 한다.
- 빌드는 네트워크 의존성, 특히 `next/font/google` fetch 여부까지 확인해야 한다.
- `package.json` 스크립트는 실제 파일 존재 여부까지 검증해야 한다.

---

## 2026-06-30: 고객 피드백 반영 방향

**확인 내용**:
20대 여성 고객은 가입 전 결과 품질, 가격 투명성, 부드러운 말투, 생활 언어 고민 선택지를 중요하게 본다.

**교훈**:
- 랜딩 첫 화면은 기능 설명보다 신뢰 형성과 샘플 확인이 우선이다.
- 결제 상품은 숨기지 말고 명확히 공개해야 한다.
- 캐릭터성은 강해도 하대감이 들면 전환을 해칠 수 있다.

---

## 2026-06-30: 9점대 신뢰 UX로 가는 다음 개선

**확인 내용**:
20대 여성 고객은 현재 서비스를 긍정적으로 보지만, 마지막 신뢰 저해 요소로 쿠키 문구, 성별 입력 이유, 현우의 위협적 톤, 가격표 접근성, 로그인 사이드바 우선순위를 지적했다.

**교훈**:
- 필수 쿠키는 마케팅식 문구보다 “필요한 쿠키만 사용”이라고 말하는 편이 신뢰를 준다.
- 민감한 입력값은 “왜 필요한지”를 입력 지점에서 바로 설명해야 한다.
- 직설 캐릭터도 겁주기보다 체크 포인트를 차분히 정리하는 쪽이 전체 브랜드 톤과 맞다.
- 시각적으로 괜찮아 보여도 스크린리더에서 붙어 읽히는 가격/배지 표현은 접근성 관점에서 개선해야 한다.
- 데스크톱 첫 화면은 로그인 요구보다 서비스 가치가 먼저 보여야 한다.

---

## 2026-07-01: 랜딩과 입력흐름의 마지막 피로도 조정

**확인 내용**:
20대 여성 고객은 현재 서비스를 긍정적으로 보고 있지만, 랜딩 인지 순서, 무료 혜택 표현, 태어난 시간 입력, 현우 톤, 컬러 피로도에서 마지막 다듬기가 필요하다고 봤다.

**교훈**:
- 내부 재화명보다 유저 행동 단위로 말해야 한다. `3별 무료`보다 `3회 무료`가 직관적이다.
- 선택 입력은 `선택`이라고만 쓰지 말고, 모르는 경우에도 진행 가능하다는 안심 문구를 붙여야 한다.
- 가격 배지는 접근성 라벨뿐 아니라 실제 시각 분리감도 중요하다.
- 카드가 가격보다 먼저 보이는 구조는 큰 문제는 아니지만, 로그인 모달 전에 무료 혜택과 가격을 충분히 인지할 수 있는지 봐야 한다.
- 상담형 앱은 오래 봐도 피곤하지 않게 따뜻한 중립색을 일부 섞는 편이 좋다.

---

## 2026-07-01: 쿠키 배너는 신뢰 요소지만 입력 차단 시 P0

**문제**:
쿠키 배너가 모바일 `/ko/reading` 390x844에서 양력/음력 선택 영역을 가렸다.

**교훈**:
- 신뢰를 위한 배너라도 핵심 입력을 막으면 전환 플로우의 P0다.
- fixed bottom UI는 하단 탭, safe-area, CTA, 폼 컨트롤과 함께 검증해야 한다.
- “다음 버튼만 보이는지”가 아니라 모든 필수/중요 입력 컨트롤의 터치 가능성을 확인해야 한다.

---

## 2026-07-01: locale 없는 법무 링크는 신뢰를 떨어뜨린다

**문제**:
쿠키 배너 개인정보 링크가 `/privacy-policy`로 이동해 현재 locale 흐름을 잃었다.

**교훈**:
- 개인정보/약관 링크는 법무 신뢰와 직결되므로 현재 언어와 경로 흐름을 유지해야 한다.
- 푸터와 쿠키 배너 등 모든 법무 링크는 동일한 i18n 라우팅 패턴을 써야 한다.

---

## 2026-07-01: 첫 캐릭터 이미지는 랜딩 LCP 후보

**문제**:
캐릭터 카드를 첫 화면으로 올리면서 첫 카드 이미지가 LCP로 잡혔고 Next priority 경고가 발생했다.

**교훈**:
- 랜딩 첫 캐릭터 이미지는 브랜드 호감과 성능 모두에 영향을 준다.
- 첫 viewport 이미지에는 priority/eager 로딩을 검토하되, 모든 카드 이미지에 남발하면 안 된다.

---

## 2026-07-01: RLS 비활성화와 owner check 누락은 즉시 P0가 된다

**문제**:
핵심 사주 테이블 RLS가 꺼진 상태에서 `readingId`만으로 analyze, chat, PDF, result, Server Action 접근이 가능할 수 있다.

**교훈**:
- 사주 앱의 리딩 데이터는 이름, 생년월일, 성별, 분석 결과를 포함하므로 일반 콘텐츠 ID보다 훨씬 민감하다.
## 2026-07-01: 모바일 쿠키 안내 inline 전환은 최종 재리뷰에서 유효했다

**확인 내용**:
개발자 수정 후 쿠키 안내가 fixed overlay가 아니라 문서 흐름 안의 `region`으로 전환됐다. `/ko`와 `/ko/reading` 390x844에서 쿠키 미동의 첫 진입을 확인했고, H1, 입력 제목, 하단 `다음` CTA를 덮지 않았다.

**교훈**:
- 쿠키 안내처럼 신뢰 목적의 UI는 모바일에서 overlay보다 inline notice가 안정적이다.
- locale-aware 법무 링크와 LCP priority는 함께 회귀 검증해야 한다.
- 문자열 기반 테스트는 최소 방어선이고, 실제 bounding box 겹침 검증을 Playwright로 추가하는 편이 안전하다.
- 현재 디자인 재리뷰 기준 P0/P1 블로킹 이슈는 없다.

---

- RLS는 켜져 있어야 하고, 서버 코드에서도 `user_id` 소유자 조건을 반복 검증하는 방어적 설계가 필요하다.
- IDOR 회귀 테스트는 공개 출시 전 필수 게이트로 둬야 한다.

---

## 2026-07-01: 결제와 내부 재화는 클라이언트 입력을 신뢰하면 안 된다

**문제**:
별 차감 API가 클라이언트 `amount`를 신뢰하고, Paddle 웹훅이 브라우저 customData의 `productType`으로 지급량을 결정할 수 있다.

**교훈**:
- 별 차감량은 서버 정책으로만 결정해야 한다.
- 음수/0/임의 amount는 잔액 조작 회귀 테스트로 막아야 한다.
- 결제 지급량은 실제 결제된 `items.price.id`나 서버 저장 checkout 정보만 신뢰해야 한다.
- 웹훅은 중복 수신을 정상 상황으로 보고 idempotency unique key를 가져야 한다.

---

## 2026-07-01: AI 비용 API는 제품 기능이 아니라 비용 경계로도 봐야 한다

**문제**:
Gemini 호출 API에 인증, rate limit, daily quota가 부족하면 무료 기능처럼 보여도 비용 폭탄 경로가 된다.

**교훈**:
- 비용 발생 API는 인증 여부와 무관하게 quota 정책이 필요하다.
- 공개 API는 사용자/IP 단위 제한과 abuse 로그를 갖춰야 한다.
- `checkRateLimit` 같은 유틸은 존재보다 실제 사용처와 테스트가 중요하다.

---

## 2026-07-01: 화면 설득 다음 병목은 첫 상담 답변 품질이다

**확인 내용**:
20대 여성 고객은 현재 랜딩 설득을 꽤 긍정적으로 봤고, 다음 핵심을 로그인 후 첫 상담 답변 품질로 짚었다.

**교훈**:
- 랜딩이 좋아져도 첫 답변이 템플릿처럼 느껴지면 상담형 앱의 약속이 깨진다.
- 첫 답변은 사주 용어보다 사용자의 고민을 생활 언어로 받아주고, 다음 대화로 자연스럽게 이어져야 한다.
- LLM 답변 테스트는 전체 문장 스냅샷보다 금지 표현, 고민 반영, 구조, 다음 질문 여부 같은 관찰 가능한 품질 기준으로 잡아야 한다.
- 캐릭터 CTA는 전환에 도움이 되지만 무료 혜택과 가격 기준을 보기 전 로그인 모달로 이어지면 작은 불신을 만들 수 있다.

---

## 2026-07-01: fixed 쿠키 배너는 위치를 바꿔도 정보 위계를 깰 수 있다

**확인 내용**:
하단 쿠키 배너가 입력/CTA를 막던 P0는 해결됐지만, 상단 fixed 배너가 모바일 `/ko` H1과 `/ko/reading` 제목 영역을 덮는 P1이 남았다.

**교훈**:
- overlay 문제는 하단에서 상단으로 옮기는 것만으로 해결되지 않는다.
- 모바일 첫 화면에서는 H1, 제목, 입력 컨트롤, CTA를 모두 가리지 않는지 봐야 한다.
- 쿠키 안내처럼 신뢰 목적의 UI는 가능하면 inline notice나 공간 예약형 배너처럼 콘텐츠 흐름 안에 넣는 편이 안정적이다.
- 개선 검증은 “버튼이 눌리는가”뿐 아니라 “첫 화면 정보 위계가 유지되는가”까지 포함해야 한다.

---

## 2026-07-01: AI 스트림 실패에서 빈 200 응답은 성공보다 나쁘다

**문제**:
Gemini quota exceeded 상태에서 `/api/saju/chat`이 `200 OK`와 빈 body를 반환했고, DB 메시지도 저장되지 않았으며, 서버 로그에는 `[saju/chat] stream error {}`만 남았다.

**교훈**:
- 스트리밍 API는 provider failure를 성공 status로 감추면 사용자가 “답변이 안 나온다”고 느낀다.
- quota/rate-limit 실패는 명확한 UI 메시지와 재시도 안내로 드러내야 한다.
- 로그에는 빈 객체가 아니라 에러 name/message/status/cause/request id를 남겨야 한다.
- 실패 상태에서 별 차감과 메시지 저장 정책이 어떻게 되는지 회귀 테스트로 고정해야 한다.
- 첫 상담 답변 품질을 보기 전에 응답 실패가 사용자에게 이해 가능한 형태로 전달돼야 한다.

---

## 2026-07-01: 빈 AI 응답 수정은 실제 인증 API와 DB까지 확인해야 닫힌다

**확인 내용**:
개발자 수정 후 임시 Supabase 유저와 인증 쿠키로 `/api/saju/chat`에 실제 요청을 보내 `text/event-stream` 응답, 비어 있지 않은 AI 상담 본문, `saju_chat_messages` user/assistant 저장, 별 3개에서 2개 차감을 확인했다.

**교훈**:
- 스트리밍 AI 회귀는 단위 테스트 통과만으로 닫지 말고 실제 인증 세션, DB 저장, 과금 차감까지 이어서 봐야 한다.
- 정상 응답 케이스에서는 body length, 상담 주제 반영, quota 메시지 부재, assistant 저장 여부를 함께 확인해야 한다.
- QA용 원격 유저와 세션 파일은 검증 직후 삭제해야 한다.

---

## 2026-07-03: Vertex ADC 전환은 provider 버전 호환성을 먼저 봐야 한다

**문제**:
`@ai-sdk/google-vertex@5.0.6`으로 실제 Gemini 호출을 시도했을 때 현재 `ai` 패키지와 model specification version이 맞지 않아 `UnsupportedModelVersionError`가 발생했다.

**해결**:
`@ai-sdk/google-vertex@3.0.146`으로 낮춘 뒤 `gemini-2.5-flash-lite` Vertex ADC 호출이 성공했다.

**교훈**:
- AI SDK provider를 추가할 때는 최신 버전보다 현재 `ai` 패키지와 같은 major/spec 세대인지 먼저 확인해야 한다.
- Google Cloud ADC 모드에서는 `GOOGLE_GENERATIVE_AI_API_KEY` 대신 `GOOGLE_VERTEX_PROJECT`, `GOOGLE_VERTEX_LOCATION`, 로컬 ADC credential이 핵심이다.
- ADC credential 파일 자체나 refresh token은 문서/로그/커밋에 남기면 안 된다.

---

## 2026-07-03: pnpm allowBuilds placeholder는 빌드를 막는다

**문제**:
`pnpm-workspace.yaml`의 `allowBuilds` 값이 `set this to true or false` placeholder로 남아 있어 `pnpm build`가 install 검증 단계에서 멈췄다.

**해결**:
`@parcel/watcher`, `@swc/core`, `esbuild`, `sharp`, `unrs-resolver`를 실제 boolean `true`로 승인했다.

**교훈**:
- pnpm 11의 build script 승인 설정은 placeholder가 아니라 명시 boolean이어야 한다.
- Next 빌드에서 필요한 네이티브 도구는 승인 목록을 코드로 고정해야 로컬/CI가 같은 방식으로 통과한다.
- `.playwright-cli/` 같은 검증 로그 폴더는 커밋하지 않도록 ignore 처리한다.
- provider failure 강제 재현은 별도 live failure 테스트나 injectable provider fake가 있어야 더 안정적으로 닫을 수 있다.

---

## 2026-07-02: AI SDK onFinish는 message 단독보다 messages fallback이 안전하다

**문제**:
사용자가 후속 질문을 여러 번 재시도했지만 `응답을 받지 못했어` 안내가 계속 표시됐다.

**확인 내용**:
AI SDK `onFinish`는 `message: this.activeResponse.state.message`와 `messages: this.state.messages`를 함께 넘긴다. 스트림 완료 타이밍이나 내부 상태 반영 방식에 따라 `message.parts`가 비어 있어도 최종 `messages` 배열의 마지막 assistant에는 텍스트가 들어 있을 수 있다.

**교훈**:
- 클라이언트 완료 판정은 `message` 하나만 보지 말고 최종 `messages` 배열의 마지막 assistant를 fallback으로 읽어야 한다.
- 빈 응답 방어문구가 반복되면 provider 실패뿐 아니라 클라이언트 텍스트 추출 오판도 의심해야 한다.
- AI stream 회귀는 서버 저장/차감뿐 아니라 클라이언트 완료 콜백의 상태 판정까지 테스트해야 한다.
- 빌드 산출물 `.next/types/* 4.ts` 같은 중복 파일은 소스 문제가 아니라 타입 체크를 오염시키는 산출물일 수 있으니 구분해야 한다.

---

## 2026-07-02: chat_used는 첫 상담과 후속 대화 품질 경계다

**문제**:
첫 답변 이후에도 `saju_readings.chat_used`가 증가하지 않아, 후속 질문이 계속 첫 상담 규칙으로 처리될 수 있었다.

**교훈**:
- 첫 상담 전용 프롬프트는 성공한 assistant 응답 이후 반드시 비활성화되어야 한다.
- `chat_used` 같은 대화 상태 필드는 답변 저장 성공과 함께 업데이트해야 후속 질문의 맥락 품질이 안정된다.
- 실패/부분 응답에서는 `chat_used`, 별 차감, 메시지 저장을 모두 진행하지 않는 편이 사용자 신뢰에 맞다.

---

## 2026-07-01: 마지막 신뢰감은 접근성과 압박 없는 톤에서 갈린다

**확인 내용**:
20대 여성 고객 재리뷰에서 남은 문제는 로그인 모달 접근성, 일부 압박감 있는 캐릭터 문구, 실제 Gemini 첫 상담 톤 확인으로 좁혀졌다.

**교훈**:
- 모달은 시각적으로 보이는 것만으로 충분하지 않고 keyboard/screen reader 기준 modal dialog 역할을 해야 한다.
- 금전/기회/경고 문구는 `돈 냄새`, `놓치면 안 돼`처럼 쓰면 상담보다 압박으로 읽힐 수 있다.
- 첫 상담 품질은 프롬프트 설계만 보지 말고 실제 Gemini 응답 캡처로 확인해야 한다.
- 대표 고민 5개를 같은 기준으로 보면 캐릭터 톤과 상담 품질의 마지막 편차를 잡기 쉽다.

---

## 2026-07-01: security definer RPC는 grant까지 봐야 한다

**문제**:
보안 핫픽스에서 Paddle 웹훅 지급을 원자적 RPC로 옮겼지만, `credit_stars_for_paddle_purchase`가 `security definer`이면서 `authenticated`에 execute grant되어 있었다.

**위험**:
- 로그인 유저가 Supabase RPC로 직접 호출하면 결제 없이 별을 충전할 수 있다.
- `p_credits`, `p_user_id`, `p_transaction_id`를 인자로 받는 함수는 클라이언트에 열리는 순간 결제 서버 검증을 우회한다.
- 기존 `decrement_star`와 `deduct_stars_for_report`도 authenticated 공개 상태면 타인 UUID 기반 차감 DoS 또는 거래 이력 오염 위험이 있다.

**교훈**:
- 웹훅 전용 RPC는 `service_role` 전용으로 둬야 한다.
- 클라이언트 직접 호출 RPC는 함수 내부에서 `auth.uid()`와 소유권을 다시 확인해야 한다.
- 보안 리뷰에서 `security definer`는 함수 본문뿐 아니라 `grant execute`까지 반드시 같이 봐야 한다.
- `authenticated`에 열어둔 별 차감 RPC도 타인 UUID 기반 차감/거래 이력 오염 위험이 있으니 `auth.uid() = p_user_id`와 reading 소유권을 함수 내부에서 강제해야 한다.

---

## 2026-07-01: suggestions도 실제 비용 API라 공개 엔드포인트로 열면 위험하다

**문제**:
`/api/saju/suggestions`가 인증, rate limit, daily quota 없이 Gemini를 호출하는 경로로 남았다.

**교훈**:
- 사용자가 보기엔 작은 추천 API라도 Gemini 호출이 있으면 비용 API다.
- 공개 route는 IP/user 기준 제한 없이는 quota 소모와 비용 폭탄으로 이어질 수 있다.
- quota 초과 요청은 provider 호출 전에 차단해야 한다.
- `checkRateLimit` 유틸은 존재 자체보다 실제 route 적용과 회귀 테스트가 중요하다.

---

## 2026-07-01: 결제 상태 전이를 클라이언트 status API에 맡기면 우회된다

**문제**:
`/api/saju/update-status`가 인증/소유자 검증은 하지만 클라이언트가 보낸 `status`를 그대로 저장한다.

**위험**:
본인 reading을 `paid`로 직접 바꾼 뒤 `/api/saju/analyze`를 호출하면 결제나 별 차감 없이 유료 분석 조건을 통과할 수 있다.

**교훈**:
- `paid`, `completed`, `generating` 같은 상태는 임의 문자열 업데이트 API로 노출하면 안 된다.
- 결제/차감 성공 후 서버 내부에서만 제한된 상태 전이를 허용해야 한다.
- 분석 API는 단순 `status === "paid"`만 보지 말고 결제/차감 증거 또는 서버 정책 전이를 신뢰해야 한다.

---

## 2026-07-02: quota 실패 처리가 좋아도 실제 상담 생성 QA는 별도로 실패로 봐야 한다

**확인 내용**:
임시 Supabase 인증 유저로 `/api/saju/chat`을 실제 호출했을 때 Gemini quota 초과로 `지금 AI 응답 한도가 잠시 막혔어. 잠시 후 다시 시도해줘.`가 스트림으로 내려왔다. 빈 200 응답은 재발하지 않았고, 메시지 저장/별 차감/chat_used 증가는 모두 막혔다.

**교훈**:
- provider 실패를 사용자에게 보이는 메시지로 바꾸는 것은 통과지만, 실제 상담 답변 생성 QA와는 별도다.
- 릴리즈 판단에서는 “에러 처리가 정상인가”와 “실제 상담이 생성되는가”를 분리해서 적어야 한다.
- Gemini free tier quota가 닫히면 대표 고민 5케이스 live QA는 실패 보고서로 남기고, billing/quota 정리 후 재실행해야 한다.
- QA 환경의 `pnpm allowBuilds` 정책 문제는 코드 실패와 분리하되, CI/로컬 검증 재현성 이슈로 추적해야 한다.

---

## 2026-07-03: 실제 상담 생성이 성공해도 첫 상담 품질 QA는 별도로 남는다

**확인 내용**:
Vertex 전환 후 임시 Supabase 인증 유저로 `/api/saju/chat`을 실제 호출했을 때 상담 응답 생성, user/assistant 메시지 저장, 별 3에서 2 차감, `chat_used` 1 증가, 제목 생성까지 확인됐다.

**교훈**:
- “실제 응답 생성”과 “응답 품질”은 같은 게이트가 아니다.
- 5케이스 live QA에서 고민 반영과 금지 표현은 통과해도, 문단 수와 다음 질문 기준이 깨질 수 있다.
- 첫 상담은 1~3문단, 마지막 질문 1개, 불필요한 이모지/외래어 금지를 프롬프트와 후처리 양쪽에서 강제해야 한다.
- 실제 API 응답은 저장/차감/상태 전이까지 확인해야 기능 통과로 볼 수 있다.

---

## 2026-07-03: 첫 상담은 권장보다 출력 형식 고정이 더 잘 먹힌다

**문제**:
`1~3문단으로 써`, `질문을 1개만 남겨` 같은 권장형 프롬프트만으로는 Gemini live QA에서 4문단 답변과 외래어가 계속 남았다.

**해결**:
첫 상담 지시를 `정확히 2문단`으로 고정하고, 마지막 문장 물음표, 금지 외래어 대체어, 낮은 max output token을 함께 적용했다.

**교훈**:
- 모바일 첫 상담처럼 형식이 중요한 답변은 “짧게”보다 “정확히 2문단”처럼 출력 구조를 명시하는 편이 안정적이다.
- 프롬프트만 바꾸고 끝내지 말고 live QA 평가표에 문단 수, 질문으로 끝남, 이모지, 외래어를 구조화해 남겨야 한다.
- 첫 상담은 후속 답변보다 토큰 상한을 낮춰야 장문의 보고서처럼 늘어지는 현상을 줄일 수 있다.

---

## 2026-07-03: live QA는 한 번의 전부 통과 기록으로 닫으면 안 된다

**확인 내용**:
수정 후 재검증에서 실제 API 응답은 2문단, 이모지 없음, 외래어 없음까지 좋아졌지만 마지막 문장이 질문으로 끝나지 않았다. 5케이스 live QA도 대부분 통과했지만 `썸/재회` 답변에 `시기(타이밍)`이 남았다.

**교훈**:
- 생성형 응답 품질은 run마다 변동하므로 이전 성공 보고서만 믿으면 안 된다.
- 실제 API 응답과 독립 5케이스 QA를 둘 다 봐야 한다.
- “마지막 문장 물음표”와 “타이밍 같은 금지 외래어 제거”는 프롬프트만으로는 흔들릴 수 있어 후처리 또는 재생성 게이트가 필요하다.
- 기능 정상과 품질 정상은 분리해서 판정해야 한다.

---

## 2026-07-03: 괄호 병기는 외래어 금지의 우회 경로다

**문제**:
Gemini 첫 상담 live QA에서 `타이밍`을 금지했는데도 `시기(타이밍)`처럼 한국어 뒤 괄호에 외래어를 붙이는 응답이 나왔다.

**해결**:
첫 상담 프롬프트에 괄호로 외래어를 병기하지 말라는 규칙을 추가하고, QA 스크립트에 `parenthesizedForeignWordPattern`을 넣어 별도 검출했다.

**교훈**:
- 금지어 대체 지시는 괄호 병기까지 막아야 실제 톤이 안정된다.
- `가벼운 외래어 없음` 같은 품질 기준은 단어 포함 검사만으로 부족하고, `시기(타이밍)` 같은 패턴 검출이 필요하다.
- 첫 상담 마지막 문장도 “물음표”뿐 아니라 실제 질문 1문장, 설명문/조언문으로 끝내지 말라는 형태로 고정해야 변동 실패가 줄어든다.

---

## 2026-07-03: 단건 API 통과와 5케이스 live QA 통과는 다르다

**확인 내용**:
재수정 후 실제 `/api/saju/chat` 인증 요청은 2문단, 질문 종료, 이모지 없음, 가벼운 외래어 없음, 고민 반영까지 통과했고, user/assistant 저장, 별 3에서 2 차감, `chat_used` 1 증가, 제목 생성도 정상이었다. 하지만 독립 Gemini 5케이스 live QA에서는 `친구/가족관계` 응답에 금지 외래어 `체크`가 1건 남았다.

**교훈**:
- 실제 API 단건 성공은 기능 정상 확인에는 충분하지만 생성형 품질 전체 통과로 보면 안 된다.
- 금지어가 여러 개일 때는 `타이밍`만 고쳐도 `체크` 같은 다른 표현이 다시 튀어나올 수 있다.
- 첫 상담 릴리즈 게이트는 실제 API 단건과 대표 고민 5케이스 live QA를 모두 통과해야 닫아야 한다.
- 외래어 금지는 프롬프트 지시만으로 흔들리므로 후처리 필터나 재생성 게이트를 고려해야 한다.

---

## 2026-07-03: 첫 상담 품질은 실제 API와 5케이스가 같이 통과해야 닫는다

**확인 내용**:
최종 재수정 후 `pnpm test`, `pnpm lint`, `pnpm test:env`, `pnpm build`가 통과했다. 실제 `/api/saju/chat` 인증 요청은 저장, 별 차감, `chat_used`, 제목 생성, 2문단, 질문 종료, 이모지 없음, 가벼운 외래어 없음까지 통과했다. 독립 Gemini 5케이스 live QA도 모든 케이스가 고민 반영, 금지 표현 없음, 1~3문단, 질문 종료, 이모지 없음, 가벼운 외래어 없음 기준을 통과했다.

**교훈**:
- 생성형 품질 버그는 실제 API 단건과 대표 5케이스 live QA가 둘 다 통과할 때 닫는다.
- 이전에 남았던 `체크` 같은 외래어는 run 변동성이 있으므로 새 날짜의 보고서로 통과 여부를 확인해야 한다.
- dev 서버를 직접 띄워 `/ko/reading` 입력 흐름과 비로그인 차단까지 확인하면 기능과 화면 리스크를 함께 줄일 수 있다.

---

## 2026-07-03: 채팅 품질과 코칭 제품 완성은 다른 문제다

**확인 내용**:
첫 상담 답변 품질은 많이 올라왔지만, 원래 PRD의 대화형 인생운 코치 경험은 오늘피드, 행동카드, 타임라인, 월간 전략 리포트, 대화 요약 리포트까지 이어져야 한다.

**교훈**:
- 첫 답변이 좋아졌다고 코칭형 사주풀이가 완성된 것은 아니다.
- 코칭형 앱은 답변 텍스트보다 `오늘 할 일`, `피할 것`, `확인 질문`, `이번 주 코칭 포인트` 같은 구조화된 다음 행동이 중요하다.
- 가격 공개와 가격 정책은 다르다. 유료 전환 전에는 채팅/리포트/백서별 별 사용량과 Paddle/약관/랜딩 source of truth를 맞춰야 한다.

---

## 2026-07-03: 배포 가능 빌드와 결제 가능 배포는 다르다

**확인 내용**:
`pnpm test`, `pnpm lint`, `pnpm build`는 통과했지만 `/api/saju/update-status` 결제 상태 우회, `/api/saju/chat` 별 차감 동시성, 운영 Supabase 마이그레이션 적용 확인, Paddle production env 검증이 남았다.

**교훈**:
- 빌드 통과는 배포 가능성의 일부일 뿐이고 결제 포함 공개 배포는 서버 신뢰 경계까지 닫혀야 한다.
- `paid` 같은 결제 상태는 클라이언트가 직접 바꿀 수 있는 일반 상태값으로 두면 안 된다.
- 별 차감은 AI 생성 전후 순서와 동시 요청을 함께 봐야 한다. 비용 발생 후 차감 실패를 로그만 남기면 정합성이 깨진다.
- DB 보안 마이그레이션은 저장소에 있는 것과 운영 DB에 적용된 것을 구분해야 한다.
- Paddle 검증은 기본 env 검사와 분리하지 말고 결제 포함 배포 게이트에서는 `REQUIRE_PADDLE_ENV=true`로 강제해야 한다.

---

## 2026-07-03: 금지어 치환은 조사와 자연어 후처리까지 확인해야 한다

**문제**:
`타이밍`을 `시기`로 일괄 치환하자 랜딩 카드에 `커리어는 시기이야`, `사업은 시기이야`, `시기을` 같은 어색한 표현이 생겼다.

**해결**:
캐릭터/랜딩 문구 테스트에 `시기이야`, `시기을`, `체크할 조건` 금지를 추가하고, 실제 모바일 Playwright 스냅샷으로 랜딩 문구를 재확인했다.

**교훈**:
- 외래어 제거는 단순 치환만으로 끝내면 조사 오류가 생긴다.
- 생성형 QA뿐 아니라 랜딩/카드/후기처럼 정적 문구도 같은 말투 기준으로 봐야 한다.
- 캐릭터 이름과 사용자 이름이 겹치는 QA는 모델이 `도윤님`처럼 캐릭터명을 유저 호칭으로 쓰게 만들 수 있으니, QA 케이스에는 별도 사용자 이름과 캐릭터명 호칭 검출이 필요하다.

---

## 2026-07-03: 문서화 후 커밋 전에는 검증 증거를 같이 남긴다

**확인 내용**:
커밋 직전 `node scripts/qa-gemini-first-consultation.mjs`, `pnpm test`, `pnpm lint`, `pnpm build`, `git diff --check`, Playwright 모바일 스모크를 확인했다.

**교훈**:
- 생성형 QA 결과는 보고서 파일과 개발일지에 같이 남겨야 다음 세션에서 현재 기준을 빠르게 회수할 수 있다.
- 모바일 UI 확인은 snapshot만이 아니라 콘솔 에러 0개까지 같이 적어두는 편이 좋다.
- 커밋 메시지는 기능명보다 실제 닫은 리스크인 첫상담 품질, 접근성, Vertex QA 안정화를 드러내야 한다.
