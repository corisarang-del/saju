# 첫상담품질 수정후 live QA

- 일시: 2026-07-03 01:11:24 KST

## 요청

- 개발자가 문제점을 수정한 뒤 실제로 제대로 작동하고 제대로 응답하는지 QA.
- 지금까지 나온 내용을 문서화하고 메모리에 저장.

## 사용한 스킬

- `powerqa`: 테스트, lint, env, build 게이트 확인.
- `playwright`: 실제 브라우저 화면과 입력 흐름 확인.
- `memory`: QA 결과와 학습 내용을 `.codex/memory`에 저장.

## 수정 확인 대상

- 지난 QA의 P1: 첫 상담 답변 품질 기준 일부 미달.
  - 4문단 출력.
  - 다음 질문 누락.
  - 이모지/외래어 섞임.

## 품질 게이트

- `pnpm test`: 통과.
  - 37개 파일 / 122개 테스트.
- `pnpm lint`: 통과.
- `pnpm test:env`: 통과.
- `pnpm build`:
  - 일반 샌드박스에서는 Turbopack 내부 포트 바인딩 권한 문제로 실패.
  - 권한 상승 환경에서는 통과.
  - Next.js 16.2.9, static pages 14개 생성 완료.

## 실제 화면 QA

- 기존 dev 서버 `http://localhost:3000` 사용.
- `/ko`:
  - 페이지 제목 `월간사주 - 먼저 챙겨주는 사주친구` 정상.
  - 콘솔은 React DevTools/HMR 개발 로그만 확인.
- `/ko/reading`:
  - 1/2 생년월일 입력 화면 정상.
  - 이름, 연도, 월, 일, 성별 입력 정상.
  - `다음` 클릭 후 2/2 고민 선택 화면 전환 정상.
  - `이직/퇴사` 선택 후 비로그인 상태에서 `분석 시작하기` 클릭 시 `로그인이 필요합니다.` 정상 표시.

## 실제 인증 채팅 API QA

- 임시 Supabase QA 유저, reading, 별 3개를 생성했다.
- 인증 쿠키를 만들어 `/api/saju/chat`에 실제 POST 요청을 보냈다.
- 요청 고민: `이직을 고민 중인데 올해 움직여도 괜찮을지 현실적으로 봐줘.`
- 응답:
  - status `200`
  - content-type `text/event-stream`
  - body length `1408`
  - quota 메시지 없음.
  - 이직/커리어 고민 반영 확인.
- DB/과금 확인:
  - `saju_chat_messages`에 user 1건, assistant 1건 저장.
  - assistant content length `649`.
  - `user_stars.balance` 3에서 2로 차감.
  - `saju_readings.chat_used` 1로 증가.
  - title `이직운, 올해 괜찮을까` 생성 확인.
- 품질 확인:
  - 문단 수 `2`: 통과.
  - 이모지 없음: 통과.
  - 가벼운 외래어 없음: 통과.
  - 마지막 문장 질문 종료: 실패.
- 정리:
  - 임시 QA 유저, reading, 채팅 메시지, 별 데이터는 스크립트 finally에서 삭제 완료.

## 실제 Gemini 첫 상담 5케이스 QA

- `node scripts/qa-gemini-first-consultation.mjs` 실행.
- 성공 보고서 생성: `docs/qa/gemini-first-consultation-qa-2026-07-02.md`
- 요약:
  - 고민 반영: 5/5 통과.
  - 금지 표현 없음: 5/5 통과.
  - 1~3문단: 5/5 통과.
  - 질문으로 끝남: 5/5 통과.
  - 이모지 없음: 5/5 통과.
  - 가벼운 외래어 없음: 4/5 통과, 1/5 확인 필요.
- 확인 필요 사례:
  - `썸/재회` 답변에 `시기(타이밍)` 표현이 남았다.

## 판단

- 실제 상담 생성, DB 저장, 별 차감, `chat_used` 증가는 정상 작동한다.
- 지난번보다 문단 수, 이모지, 외래어 억제는 크게 좋아졌다.
- 하지만 완전 통과는 아니다.
- 남은 P1:
  - 실제 API 응답이 마지막 질문으로 끝나지 않을 수 있다.
  - live QA 5케이스 중 1개에서 금지 외래어 `타이밍`이 남았다.
- 첫 상담 품질을 릴리즈 통과로 보려면 실제 API 응답과 5케이스 live QA가 모두 질문 종료/외래어 금지 기준을 통과해야 한다.
