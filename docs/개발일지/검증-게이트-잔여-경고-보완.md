# 검증 게이트 잔여 경고 보완

- 일시: 2026-06-30 23:53:34 KST

## 사용한 접근

- `systematic-debugging` 흐름으로 테스터가 남긴 경고를 재현하고 원인을 좁혔다.
- `verification-before-completion` 기준으로 완료 전 lint, test, build, typecheck를 다시 확인했다.
- TDD 방식으로 환경변수 검사와 Next proxy convention 회귀 테스트를 먼저 추가한 뒤 구현했다.

## 변경 내용

- `scripts/check-env.js`
  - Paddle 환경변수는 기본 검증에서 선택 경고를 내지 않도록 정리했다.
  - `REQUIRE_PADDLE_ENV=true`일 때만 Paddle 관련 키를 필수로 검사하도록 바꿨다.
- `src/middleware.ts`를 `src/proxy.ts`로 전환했다.
  - Next 최신 proxy convention을 사용해서 middleware convention deprecated 경고를 제거했다.
- lint warning 28개를 정리했다.
  - 미사용 import/변수 제거
  - 불필요한 eslint-disable 제거
  - ternary expression 경고가 나던 shim 코드 정리
  - 사용하지 않는 catch 바인딩 제거
- 회귀 테스트를 추가했다.
  - `scripts/check-env.test.mjs`
  - `src/lib/next/proxy-convention.test.ts`

## 검증 결과

- `pnpm lint`: 통과, 경고 없음
- `pnpm test:env`: 통과, Paddle placeholder 경고 없음
- `pnpm test`: 19개 파일, 44개 테스트 통과
- `pnpm build`: 통과, middleware convention deprecated 경고 없음
- `./node_modules/.bin/tsc --noEmit`: 통과
- `git diff --check`: 통과
