# 가격정책 스타터 2900 월구독 50별 구현

## 변경 내용
- `src/lib/monthly-saju/pricing.ts`의 스타터 상품을 `10별 2,900원`으로 변경했다.
- 스타터 설명을 `무료 3회 뒤 조금 더 이어보고 싶을 때`로 추가해 첫 가벼운 결제 포지션을 명확히 했다.
- 월간 멤버십 지급량을 `40별`에서 `50별`로 변경하고 설명 문구도 같이 맞췄다.
- 코인샵 기본 선택 상품을 `stars70`에서 `stars10`으로 바꿔 스타터 상품이 먼저 선택되게 했다.
- 약관/환불정책은 가격 숫자를 직접 쓰지 않고 결제 안정화 안내와 환불 조건만 설명하므로 가격 불일치 수정 대상은 없었다.

## TDD 기록
- 먼저 가격 정책 테스트를 추가/수정했다.
- 실패 확인:
  - `publishes_starter_price_as_2900_won`
  - `publishes_monthly_membership_with_50_stars`
  - `publishes_starter_existing_and_subscription_products`
  - `uses_same_price_policy_for_landing_terms_jsonld_and_paddle_mapping`
- 구현 후 타깃 테스트가 통과했다.

## 검증
- `./node_modules/.bin/vitest run src/lib/monthly-saju/pricing.test.ts src/lib/paddle/credit-grant.test.ts src/lib/paddle/membership.test.ts`: 통과, 3 files / 14 tests.
- `./node_modules/.bin/vitest run`: 통과, 55 files / 252 tests.
- `./node_modules/.bin/tsc --noEmit`: 통과.
- `./node_modules/.bin/eslint .`: 통과.
- `./node_modules/.bin/next build`: 샌드박스에서는 Turbopack 포트 바인딩 제한으로 실패했지만, 권한 밖 재실행에서 production build 통과.
- `rg -n "3,900|3900|40별|40 별|매월 40" src messages scripts`: 남은 옛 가격/지급량 문구 없음.

## 남은 운영 작업
- Paddle Dashboard에서 `stars10` price id가 실제 `2,900원` 상품을 가리키도록 교체해야 한다.
- 월간 멤버십 price id가 실제 `9,900원 / 50별` 정책 상품을 가리키는지 확인해야 한다.
- Vercel production env의 Paddle product/price id를 새 정책과 맞춘 뒤 signed webhook live QA를 다시 돌려야 한다.
- `stars10` 첫 결제 1회 제한은 이번 코드 변경에는 넣지 않았고, UI/문구 포지셔닝만 스타터로 정리했다.
