# 배포반영후 PC/폰 디자인개선 재리뷰 반영

## 참고 문서

- `/Users/apple/Desktop/test_githup/saju/docs/pm/배포반영후-PC-폰-디자인개선-재리뷰-20260724.md`

## 사용 스킬

- `design-taste-frontend`: 모바일/PC 반응형 UI 마감 기준 확인
- `playwright`: 실제 브라우저에서 모바일/PC 스크린샷 확인

## TDD

- `src/lib/design/design-audit-regression.test.ts`에 남은 재리뷰 기준을 먼저 추가했다.
- 실패 확인:
  - PC 캐러셀 좌측 edge fade 강화
  - 모바일 첫 캐릭터 CTA fold 내 여백 확보
  - 모바일 H1 의미 단위 줄바꿈
  - 태어난 시간 select placeholder 대비 보강

## 구현 내용

- `src/app/[locale]/page.tsx`
  - H1의 `캐릭터를 고르면`도 `inline-block`으로 감싸 모바일에서 `고르/면`처럼 단어 중간이 갈라지지 않게 했다.

- `src/components/saju/landing/CharacterCards.tsx`
  - 모바일 카드 폭을 `66vw`에서 `62vw`로 줄이고 최대 폭을 `250px`에서 `236px`로 줄였다.
  - 모바일 이미지 비율을 `4/5`에서 `5/6`으로 낮춰 첫 카드 CTA가 하단 탭 위에서 더 여유 있게 보이도록 했다.
  - 모바일에서는 짧은 인용문을 숨겨 카드 하단 CTA 영역을 더 안정적으로 확보했다.
  - PC 좌측 mask fade를 `transparent_48px`, `black_88px`까지 넓혀 사이드바 옆 이전 카드 조각의 존재감을 줄였다.

- `src/components/saju/input/BirthDateForm.tsx`
  - 시간 선택 select trigger에 `text-[#191F28]`와 `data-[placeholder]:text-[#667085]`를 추가해 placeholder 대비를 올렸다.

## 검증

- `./node_modules/.bin/vitest run src/lib/design/design-audit-regression.test.ts` 통과
- `./node_modules/.bin/vitest run` 통과: 53 files, 236 tests
- `./node_modules/.bin/tsc --noEmit` 통과
- `./node_modules/.bin/eslint .` 통과
- `./node_modules/.bin/next build` 통과

## 시각 확인

- 로컬 `http://localhost:3001/ko`에서 Playwright 확인
- 390x844 모바일:
  - H1이 `캐릭터를 고르면`, `오늘의 흐름부터`, `먼저 정리해줄게` 의미 단위로 줄바꿈된다.
  - 첫 카드 `대화하기` CTA가 하단 탭과 겹치지 않고 보인다.
- 1280x720 PC:
  - 좌측 캐러셀 peek가 약한 edge fade 안에 들어가 사이드바와 시각 충돌이 줄었다.

