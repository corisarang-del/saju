# 테스터 인수인계 검증 이슈 수정

## 일시

- 2026-06-30 23:24:44 KST

## 작업 내용

- 테스터 인수인계 문서의 실패 항목을 재현했다.
- `next/font/google` 의존을 제거하고 OS 한글 폰트 스택으로 빌드 중 외부 Google Fonts fetch를 제거했다.
- React 19 린트 에러를 유발하던 동기 `setState` 패턴을 초기값, 파생값, `useSyncExternalStore` 기반 구조로 변경했다.
- `scripts/check-env.js`와 동작 테스트를 추가해 `test:env` 경로 불일치를 복구했다.
- 린트 에러였던 `any`, `prefer-const`, 내부 링크 `<a>` 사용 문제를 수정했다.
- `pnpm` 런처 문제는 프로젝트 로컬 설정으로 해결되지 않는 것을 확인했고, 전역 pnpm 실행 환경 이슈로 남겼다.

## 남은 확인

- 전역 `pnpm` 런처가 `pnpm --version`, `pnpm test`, `pnpm lint`, `pnpm test:env`에서 출력 없이 걸리거나 `[ERROR] fetch failed`로 종료된다. `/opt/homebrew/bin/pnpm` 자체 실행도 같은 증상이라 프로젝트 코드 바깥 환경 문제로 보인다.

## 검증 결과

- `./node_modules/.bin/vitest run`: 16 files / 38 tests 통과.
- `./node_modules/.bin/eslint .`: exit 0, 에러 0개, 기존 경고 28개.
- `./node_modules/.bin/tsc --noEmit`: 통과.
- `./node_modules/.bin/next build`: 통과. sandbox에서는 Turbopack 포트 바인딩이 막혀 실패했고, 권한 상승 실행에서는 통과.
- `node scripts/check-env.js`: 필수 환경변수 통과, Paddle 선택 환경변수 placeholder 경고.
- `git diff --check`: 통과.
- `pnpm --version`, `pnpm test`, `pnpm lint`, `pnpm test:env`: 전역 pnpm 런처가 응답 없이 걸리거나 fetch 실패로 종료되어 미통과.
