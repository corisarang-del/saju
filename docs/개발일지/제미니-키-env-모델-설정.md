# 제미니 키 env 모델 설정

- 샘플 env 파일에 실제 API 키가 들어간 상태를 확인했다.
- 커밋될 수 있는 `.env.example`에서 실제 키를 제거하고 placeholder로 되돌렸다.
- API 키 흔적이 남을 수 있는 `.env.example.swp` 임시 파일을 삭제했다.
- 코드가 `GOOGLE_GENERATIVE_AI_API_KEY`와 `gemini-2.5-flash-lite`를 사용하고 있음을 확인했다.

