# 배포반영후 PC/폰 디자인개선 최종재리뷰 반영

## 참고 문서

- `/Users/apple/Desktop/test_githup/saju/docs/pm/배포반영후-PC-폰-디자인개선-최종재리뷰-20260724.md`

## 사용 스킬

- `design-taste-frontend`: 모바일 입력폼 명료도와 PC 캐러셀 마감 기준 확인.

## TDD

- `src/lib/design/design-audit-regression.test.ts`를 먼저 수정해 실패를 확인했다.
- 실패 기준:
  - PC 캐러셀 edge fade가 `transparent_64px`, `black_116px`까지 넓어져야 한다.
  - 시간 선택 placeholder는 `data-[placeholder]:text-[#4E5968]`, `data-[placeholder]:font-medium`으로 더 선명해야 한다.

## 구현 내용

- `src/components/saju/input/BirthDateForm.tsx`
  - 태어난 시간 select trigger의 placeholder 색상을 `#667085`에서 `#4E5968`로 올리고 `font-medium`을 추가했다.
  - 날짜 입력 placeholder보다 약해 보이던 select placeholder를 입력폼 전체 톤에 더 맞췄다.

- `src/components/saju/landing/CharacterCards.tsx`
  - PC 캐러셀 왼쪽 mask fade를 `transparent_48px/black_88px`에서 `transparent_64px/black_116px`로 넓혔다.
  - 1280px 전후에서 사이드바 옆 이전 카드 조각의 존재감을 더 낮췄다.

## 검증

- `./node_modules/.bin/vitest run src/lib/design/design-audit-regression.test.ts` 통과.
- `./node_modules/.bin/vitest run` 통과: 55 files, 244 tests.
- `./node_modules/.bin/tsc --noEmit` 통과.
- `./node_modules/.bin/eslint .` 통과.
- `./node_modules/.bin/next build` 통과.
- 일반 샌드박스 빌드는 Turbopack 포트 바인딩 제한으로 실패했지만, 권한 있는 실제 빌드는 통과했다.

