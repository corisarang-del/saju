# 가격정책 현우카드 수정내용 문서화 커밋푸시

## 이번 묶음 변경
- `stars10` 스타터 상품을 `10별 2,900원`으로 변경했다.
- 월간 멤버십을 `월 9,900원 / 매월 50별`로 변경했다.
- 랜딩/FAQ/JSON-LD/Paddle mapping이 같은 가격 상수에서 파생되는지 테스트로 묶었다.
- 코인샵 기본 선택 상품을 `stars10`으로 바꿔 무료 3회 이후 첫 결제 흐름이 가볍게 보이도록 했다.
- 배포 스크린샷에서 현우 카드 좌측이 덜 보이던 문제를 캐러셀 mask 분기로 수정했다.

## 현우 카드 수정 요약
- 첫 카드 상태에서도 왼쪽 edge fade가 적용되어 현우 카드 왼쪽이 투명 처리되던 것이 원인이었다.
- `activeIndex === 0`일 때는 왼쪽 mask를 `black_0`부터 시작하게 했다.
- `activeIndex > 0`일 때만 기존 `transparent_64px -> black_116px` edge fade를 적용한다.

## 검증 완료
- 가격/Paddle 타깃 테스트 통과.
- 디자인 회귀 테스트 통과.
- 전체 `vitest`: 55 files / 253 tests 통과.
- `tsc --noEmit` 통과.
- `eslint .` 통과.
- `next build`는 샌드박스 포트 바인딩 제한으로 한 번 실패했지만, 권한 밖 재실행에서 production build 통과.

## 남은 운영 확인
- Paddle Dashboard와 Vercel production env의 실제 price id를 새 가격 정책과 맞춰야 한다.
- Vercel production 배포 후 `https://monthlysaju.vercel.app/ko`에서 현우 카드 좌측 fade가 사라졌는지 다시 확인해야 한다.
