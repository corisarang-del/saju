# 디자인 개선 수정후 리뷰

- 일시: 2026-07-01 KST
- 요청: 개발자가 디자인 개선점을 수정한 뒤 디자인 리뷰
- 범위: 이전 디자인 감사 티켓 기준으로 쿠키 배너, 브랜드 색상, 입력 폼, 캐러셀, 후기, 접근성을 재검토
- 검증:
  - Playwright로 `/ko`, `/ko/reading` 모바일 화면 확인
  - `./node_modules/.bin/vitest run src/lib/design/design-audit-regression.test.ts src/lib/content/trust-copy.test.ts` 통과
  - `pnpm exec vitest ...`는 기존 환경 이슈와 동일하게 `[ERROR] fetch failed`로 실패

## 리뷰 요약

- 브랜드 색상 통합, 후기 톤 완화, 캐러셀 접근성/모션 제어, 입력 라벨 보강은 개선됨.
- 하지만 모바일에서 쿠키 배너가 `달력 구분` 입력 영역과 랜딩 카드 CTA를 여전히 가린다.
- 쿠키 배너 개인정보 링크가 locale prefix 없이 `/privacy-policy`로 이동한다.
- 첫 화면 캐릭터 이미지가 LCP가 되면서 Next.js LCP 경고가 발생한다.
