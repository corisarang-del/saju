# 수정후 실사용 QA 20260710 2차

- 날짜: 2026-07-10
- 역할: QA 테스터
- 요청: 개발자가 수정한 뒤 실제로 제대로 작동하고 제대로 응답하는지 다시 검증하고, 결과를 문서화하고 메모리에 저장해달라는 요청이었어.

## 결론

완전 통과는 아니야. 자동 테스트, 린트, 권한 올린 빌드, 기본 HTTP, OAuth 시작, 리포트 차감, 월간 리포트 중복 차감 방지, 채팅 동시 요청 방어, 유료 첫 채팅 품질은 통과했어. 하지만 Gemini 단독 live QA에서 1케이스가 질문으로 끝나지 않았고, 실제 API 무료 첫 채팅도 질문으로 끝나지 않았는데 저장과 별 차감이 발생했어.

## 자동 게이트

- 관련 회귀 테스트: 7 files / 40 tests 통과.
- `pnpm test`: 49 files / 179 tests 통과.
- `pnpm lint`: 통과.
- `pnpm test:env`: 필수 환경변수 검사 통과.
- `pnpm build`: 샌드박스에서는 Turbopack 포트 바인딩 제한으로 실패했지만, 권한 올려 재실행하니 통과.
- `git diff --check`: 통과.

## Gemini live QA

`node scripts/qa-gemini-first-consultation.mjs`를 실제 Gemini/Vertex 호출로 실행했고, 리포트는 `docs/qa/gemini-first-consultation-qa-2026-07-10.md`에 갱신됐어.

- 6케이스 중 5케이스는 전체 기준 통과.
- `이직/퇴사` 케이스는 `질문으로 끝남`이 `확인필요`.
- 해당 답변 마지막 문장: `이 내용을 바탕으로 다음에 어떤 부분을 개선하거나 변화시키고 싶은지 다음 질문에 답해주시면 더 깊이 이야기 나눌 수 있습니다.`
- 이모지, 외래어, 영어 혼합, 캐릭터명 호칭 기준은 통과.

## 실제 HTTP와 브라우저

- `GET /ko`: HTTP 200.
- `GET /api/auth/google?next=/ko/reading`: 307, Supabase Google OAuth URL로 리다이렉트됨.
- Playwright 실제 브라우저 `/ko`: 페이지 제목 `월간사주 - 먼저 챙겨주는 사주친구`, 스냅샷 생성 성공.
- 콘솔 로그: React DevTools 안내와 HMR 연결 로그뿐이고 런타임 에러는 없었어.

## 실제 API QA

임시 Supabase 테스트 유저를 만들고 SSR 세션 쿠키를 넣어서 로컬 API를 실제 호출했어. 테스트 뒤 유저, 리딩, 메시지, 별, 거래 로그는 삭제했어.

### 결제/차감

- `/api/saju/update-status`에 `status: paid` 직접 요청:
  - 응답: 403 `Forbidden status transition`
  - 리딩 상태: `pending` 유지
  - 판정: 통과
- `/api/saju/deduct-stars`:
  - 응답: 200 `{ "balance": 5, "amount": -5 }`
  - 리딩 상태: `paid`
  - 거래 로그: `amount=-5`, `balance_after=5`, `type=report`
  - 판정: 통과
- `/api/monthly-saju/deduct-monthly-report`:
  - 첫 호출: 200 `{ "balance": 2, "amount": -3, "alreadyUnlocked": false }`
  - 두 번째 호출: 200 `{ "balance": 2, "amount": 0, "alreadyUnlocked": true }`
  - 판정: 통과

### 채팅

- 유료 채팅 동시 요청:
  - 응답 상태: `[200, 409]`
  - 409 본문: `chat_generation_in_progress`
  - 별 잔액: 10에서 9로 1개만 차감
  - `chat_used`: 1
  - 제목 생성: `이직운 퇴사운`
  - 동시 요청 방어 판정: 통과
- 유료 채팅 실제 응답:
  - 길이: 244자
  - 문단 수: 2
  - 질문 종료: 통과
  - 이모지 없음: 통과
  - 영어 혼합 없음: 통과
  - 고민 반영: 통과
  - 판정: 통과
- 무료 채팅 실제 응답:
  - 상태: 200
  - 길이: 304자
  - 문단 수: 2
  - 이모지 없음: 통과
  - 영어 혼합 없음: 통과
  - 고민 반영: 통과
  - 질문 종료: 실패
  - 마지막 문장: `혹시 지금 회사에서 네 역량을 발휘하기 어려운 점이 있다면, 그걸 적어보는 것도 좋고.`
  - 별 잔액: 3에서 2
  - `chat_used`: 1
  - 판정: 실패

## 남은 문제

1. Gemini 단독 QA에서 `이직/퇴사` 케이스가 질문으로 끝나지 않아 `확인필요`로 떨어졌어.
2. 실제 `/api/saju/chat` 무료 첫 응답도 질문으로 끝나지 않았는데 저장되고 별 차감, `chat_used` 증가가 발생했어.
3. 저장 전 품질 게이트가 무료 첫 상담에도 동일하게 걸리는지 확인해야 해. 지금 결과상 무료 첫 상담의 질문 종료 누락은 막히지 않았어.

## 개발자에게 전달할 포인트

- 유료 첫 채팅의 4문단/`MC` 문제는 이번 샘플에서는 재현되지 않았고 통과했어.
- 무료 첫 상담도 첫 상담 품질 게이트 대상이어야 해. 마지막 문장 물음표 누락이면 저장/차감/`chat_used` 증가를 막아야 해.
- Gemini 단독 QA도 아직 확률적으로 질문 종료 누락이 발생하므로, 프롬프트만 믿지 말고 실패 응답 재생성이나 후처리를 고려해야 해.
