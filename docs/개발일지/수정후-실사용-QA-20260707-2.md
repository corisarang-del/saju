# 수정후 실사용 QA 20260707 2차

- 일시: 2026-07-07 15:28:47 KST

## 요청

- 개발자가 문제점을 수정한 뒤 실제로 제대로 작동하고 제대로 응답하는지 QA.
- 지금까지 나온 내용을 문서화하고 메모리에 저장.

## 사용한 스킬

- `powerqa`: 테스트, lint, env, build 게이트 확인.
- `playwright`: 실제 브라우저 화면 확인 시도.
- `memory`: QA 결과와 학습 내용을 `.codex/memory`에 저장.

## 자동 게이트

- `pnpm test`:
  - 전체 실행은 실패.
  - 결과: 43개 파일 중 1개 실패, 150개 테스트 중 1개 실패.
  - 실패: `src/lib/auth/oauth.test.ts > uses_forwarded_host_when_origin_header_is_missing`
  - 증상: 전체 실행 중 해당 테스트가 5초 타임아웃.
  - 단독 실행: `pnpm exec vitest run src/lib/auth/oauth.test.ts --testNamePattern 'uses_forwarded_host_when_origin_header_is_missing'`는 통과.
  - 판단: 단독 로직은 정상이나 전체 테스트 러너가 과도하게 느려지며 타임아웃이 발생한다.
- `pnpm lint`:
  - `eslint`가 2분 이상 출력 없이 멈춰 중단했다.
  - 판단: 이번 QA 기준 lint 완료 확인 불가.
- `pnpm test:env`: 통과.
- `pnpm build`:
  - 3분 이상 출력 없이 멈춰 중단했다.
  - 판단: 이번 QA 기준 build 완료 확인 불가.

## 실제 Gemini 첫 상담 6케이스 QA

- `node scripts/qa-gemini-first-consultation.mjs`를 네트워크 권한으로 실행했다.
- 성공 보고서 생성: `docs/qa/gemini-first-consultation-qa-2026-07-07.md`
- 요약:
  - 고민 반영: 6/6 통과.
  - 금지 표현 없음: 6/6 통과.
  - 1~3문단: 6/6 통과.
  - 질문으로 끝남: 6/6 통과.
  - 이모지 없음: 6/6 통과.
  - 가벼운 외래어 없음: 6/6 통과.
  - 캐릭터명 호칭 없음: 6/6 통과.

## 실제 인증 API QA

### 상태 전이 차단

- `/api/saju/update-status`에 `status: paid` 직접 승격 요청을 보냈다.
- 결과:
  - status `403`
  - body `{"error":"Forbidden status transition"}`
  - reading status는 `pending` 유지.
- 판단: 클라이언트의 유료 상태 직접 승격 차단은 정상이다.

### 종합 리포트 별 차감

- pending reading과 별 10개를 가진 임시 Supabase QA 유저로 `/api/saju/deduct-stars`를 호출했다.
- 결과:
  - status `200`
  - body `{"balance":5,"amount":-5}`
  - reading status는 `paid`로 변경.
  - `star_transactions`에 `amount: -5`, `balance_after: 5`, `type: report` 저장.
- 판단: 직전 QA에서 실패했던 `deduct_stars_for_report` RPC 문제는 실제 환경에서 해결됐다.

### 월간 리포트 별 차감

- `/api/monthly-saju/deduct-monthly-report`에 `month: 2026-07`로 호출했다.
- 첫 호출:
  - status `200`
  - body `{"balance":2,"amount":-3,"alreadyUnlocked":false}`
- 두 번째 호출:
  - status `200`
  - body `{"balance":2,"amount":0,"alreadyUnlocked":true}`
- 판단: 월간 리포트는 첫 호출만 3별 차감하고, 중복 호출은 이미 해제된 상태로 처리한다.

### 채팅 중복 요청과 저장/차감

- 별 10개 보유 상태의 completed reading으로 `/api/saju/chat` 첫 요청을 보내고, 응답 완료 전 두 번째 요청을 보냈다.
- 결과:
  - 첫 요청 status `200`, content-type `text/event-stream`.
  - 두 번째 요청 status `409`.
  - 두 번째 body: `{"error":"chat_generation_in_progress","message":"이미 답변을 준비하고 있어. 잠시 후 다시 시도해줘.",...}`
  - `saju_chat_messages`에는 user 1건, assistant 1건만 저장.
  - 별은 10에서 9로 1개만 차감.
  - `chat_used`는 1로 증가.
  - title `올해 이직운 풀이` 생성.
- 판단: 중복 생성 방지와 중복 차감 방지는 정상이다.

### 실제 채팅 응답 품질

- 별 3개 무료 첫 상담 케이스:
  - status `200`
  - user/assistant 저장 정상.
  - 별 3에서 2로 차감.
  - `chat_used` 1 증가.
  - 문단 수 `2`, 마지막 질문 종료, 이모지 없음, 외래어 없음, 커리어 고민 반영 통과.
- 별 10개 보유 paid prompt 첫 상담 케이스:
  - status `200`
  - user/assistant 저장 정상.
  - 별 10에서 9로 차감.
  - `chat_used` 1 증가.
  - 마지막 질문 종료, 외래어 없음, 커리어 고민 반영 통과.
  - 문단 수 `5`로 실패.
  - 이모지 포함으로 실패.
  - 실제 응답에 `토끼` 이모지가 포함됐다.
- 판단: 무료 첫 상담은 정상이나, paid prompt 경로의 첫 상담은 아직 첫 상담 품질 기준을 깬다.

## OAuth 시작 라우트 QA

- `GET /api/auth/google?next=/ko/reading` 호출.
- 결과:
  - status `307`.
  - Supabase Google authorize URL로 redirect.
  - `redirect_to=http://localhost:3000/auth/callback?next=%2Fko%2Freading` 포함 확인.
- 판단: OAuth 시작 라우트는 로컬 기준 정상적으로 redirect URL을 만든다.

## 화면 QA

- `/ko` HTTP HEAD:
  - status `200`.
- Playwright:
  - `/ko/reading` open이 1분 이상 응답 없이 멈춰 중단했다.
  - 판단: 이번 라운드에서는 브라우저 입력 흐름 완료 확인 불가.

## 판단

- 직전 실패였던 종합 리포트 별 차감 RPC는 실제 환경에서 해결됐다.
- 월간 리포트 별 차감과 중복 해제도 정상이다.
- 무료 첫 상담과 6케이스 Gemini live QA는 통과했다.
- 남은 실제 문제:
  - P1: paid prompt 경로의 첫 상담이 5문단과 이모지 포함으로 품질 기준을 깬다.
  - P1: 전체 `pnpm test`가 특정 OAuth 테스트 타임아웃으로 실패한다. 단독 실행은 통과하므로 전체 테스트 성능/타임아웃 문제를 봐야 한다.
  - P1: `pnpm lint`와 `pnpm build`가 장시간 무응답으로 완료 확인이 안 됐다.
  - P2: Playwright `/ko/reading` open이 장시간 무응답으로 화면 입력 흐름을 이번 라운드에서 완료 확인하지 못했다.
- 이번 수정본은 전체 QA 통과로 닫으면 안 된다. paid prompt 첫 상담 품질과 로컬 게이트 무응답 문제를 추가로 고쳐야 한다.
