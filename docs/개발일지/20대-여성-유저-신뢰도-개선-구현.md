# 20대 여성 유저 신뢰도 개선 구현

## 일시

- 2026-06-30 23:34:35 KST

## 작업 내용

- `design-taste-frontend` 스킬 기준으로 입력폼 신뢰 문구와 접근성 라벨을 정리했다.
- 생년월일 입력 전 개인정보 안심 문구를 추가하고, 삭제 요청 가능 문구를 명시했다.
- 년/월/일 입력칸에 각각 시각 라벨, `aria-label`, 단위 텍스트, 예시 보조문구를 추가했다.
- 후기 원본 문구를 `src/lib/content/trust-copy.ts`로 분리하고, 과장된 투자·성공 확정 표현을 자연스러운 고민 중심 문구로 바꿨다.
- `messages/*.json`에서 이전 SaaS, SDS, 화학, Imazify, Amazon 관련 문구를 제거하고 월간사주 문맥으로 정리했다.
- 캐릭터 원본 프롬프트에서 `니 사주`, `형이`, `어이 동생`, `당하는 거야` 같은 하대감 표현을 제거했다.

## 테스트

- `src/lib/content/trust-copy.test.ts`: 개인정보 안심 문구와 후기 과장 표현 방지.
- `src/lib/content/message-hygiene.test.ts`: 다국어 메시지의 이전 SaaS 키워드 회귀 방지.
- `src/lib/saju/characters.test.ts`: 화면 노출용 정제본과 원본 프롬프트 모두의 말투 회귀 방지.

## 남은 확인

- 전역 `pnpm` 런처 문제는 이전 검증과 동일하게 별도 환경 이슈로 남아 있어 로컬 바이너리 기준으로 검증했다.

## 검증 결과

- `./node_modules/.bin/vitest run`: 18 files / 42 tests 통과.
- `./node_modules/.bin/eslint .`: exit 0, 에러 0개, 기존 경고 28개.
- `./node_modules/.bin/tsc --noEmit`: 통과.
- `./node_modules/.bin/next build`: 통과. sandbox에서는 Turbopack 권한 문제가 있어 권한 상승으로 검증했다.
- `rg` 검색: `messages/*.json`에서 SDS/화학/이전 SaaS 키워드 없음.
- `rg` 검색: 후기와 캐릭터 원본에서 금지 말투/과장 후기 문구 없음.
- `git diff --check`: 통과.
