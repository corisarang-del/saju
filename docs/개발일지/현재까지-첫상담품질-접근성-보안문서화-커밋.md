# 현재까지 첫상담품질 접근성 보안문서화 커밋

- 일시: 2026-07-03 20:56:28 KST

## 작업 요약

- Gemini 첫 상담 live QA를 `썸/재회`, `이직/퇴사`, `돈 모으기`, `번아웃`, `친구/가족관계`, `사업/창업` 6케이스로 확장했다.
- QA 표에 `캐릭터명 호칭 없음`을 추가해 캐릭터 이름을 사용자 이름처럼 부르는 응답을 잡게 했다.
- 첫상담 프롬프트와 실제 채팅 API 프롬프트에 정확히 2문단, 마지막 실제 질문 1문장, 외래어/이모지/괄호 외래어 병기 금지, 안내문 종료 금지를 강화했다.
- `타이밍`, `체크`, `플랜`, `솔루션`은 각각 `시기`, `확인`, `계획`, `방법`으로 바꾸도록 고정했다.
- 캐릭터/랜딩 문구의 `타이밍`을 정리했고, `시기이야`, `시기을`, `체크할 조건` 같은 치환 부작용을 테스트로 막았다.
- `/ko/reading` 모바일 390x844 흐름에서 쿠키 안내, 개인정보/태어난 시간/성별 안내, 입력 1/2 -> 고민 2/2 전환, 비로그인 로그인 필요 메시지를 확인했다.
- `/ko` 랜딩 로그인 모달에서 `dialog` 노출, Google 로그인/닫기 버튼 접근성 이름, Escape 닫기, 포커스 복귀를 확인했다.
- 가격/코칭 MVP 갭과 배포보안 릴리즈게이트 문서를 추가했다.
- `pnpm-workspace.yaml`의 `packages` 누락 문제를 복구했다.

## 최신 검증

- `node scripts/qa-gemini-first-consultation.mjs`: 6케이스 전부 통과.
- `pnpm test`: 37 files / 125 tests 통과.
- `pnpm lint`: 통과.
- `pnpm build`: 통과.
- `git diff --check`: 통과.
- Playwright 콘솔: 에러 0개, 경고 0개.

## 남은 주의사항

- 실제 인증 앱 API 전체 경로 6케이스 반복 호출은 아직 별도 자동화가 필요하다.
- 결제 포함 공개 배포는 `/api/saju/update-status` 결제 상태 우회, 별 차감 동시성, 운영 Supabase 마이그레이션, Paddle production 환경변수 검증이 끝난 뒤 진행해야 한다.
