# 실사용 QA 빈 AI응답 수정 요청

- 일시: 2026-07-01 00:53:01 KST
- 사용자 요청 요약: `docs/pm/실사용-qa-빈-ai응답-개발자-전달.md` 문서를 바탕으로 Gemini 실패/쿼터 초과 시 `/api/saju/chat`이 `200 OK`와 빈 본문을 반환하는 문제를 수정해달라고 요청함.

## 원문 요지

- Gemini quota exceeded 상태에서 채팅 API가 빈 문자열 body와 `200 OK`를 반환하면 안 된다.
- 사용자는 명확한 실패 UI와 재시도 버튼을 봐야 한다.
- 서버 로그에는 quota/rate-limit 원인을 추적 가능한 형태로 남겨야 한다.
- 빈 assistant 메시지를 성공으로 저장하거나 별을 차감하면 안 된다.
- 클라이언트는 `200 + empty body`를 성공 완료로 처리하면 안 된다.
